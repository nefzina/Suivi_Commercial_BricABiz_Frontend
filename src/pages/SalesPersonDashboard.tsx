import { useState } from "react";
import type { User } from "../types/User";
import { KpiCards } from "../components/KpiCards";

export function SalesPersonDashboard() {
//   const [user, setUser] = useState<User>({
    // fullname: "",
    // email: "",
    // role: "",
    // zoneId: {
    //   name: "",
    //   regionCode: "",
    // },
    // active: true,
    // createdAt: Date,
    // updatedAt: Date,
//   });

  return (
    <div className="salesPersonDashboard"> salesPersonDashboard
       {/* <h3>Bonjour {user.fullname},</h3>
       <KpiCards fromDate={fromDate} toDate={}/> */}
    </div>
  );
}
